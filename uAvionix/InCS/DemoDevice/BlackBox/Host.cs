
/**							Apache License
Version 2.0, January 2004
http://www.apache.org/licenses/

TERMS AND CONDITIONS FOR USE, REPRODUCTION, AND DISTRIBUTION

1. Definitions.

"License" shall mean the terms and conditions for use, reproduction,
and distribution as defined by Sections 1 through 9 of this document.

"Licensor" shall mean the copyright owner or entity authorized by
the copyright owner that is granting the License.

"Legal Entity" shall mean the union of the acting entity and all
other entities that control, are controlled by, or are under common
control with that entity. For the purposes of this definition,
"control" means (i) the power, direct or indirect, to cause the
direction or management of such entity, whether by contract or
otherwise, or (ii) ownership of fifty percent (50%) or more of the
outstanding shares, or (iii) beneficial ownership of such entity.

"You" (or "Your") shall mean an individual or Legal Entity
exercising permissions granted by this License.

"Source" form shall mean the preferred form for making modifications,
including but not limited to software source code, documentation
source, and configuration files.

"Object" form shall mean any form resulting from mechanical
transformation or translation of a Source form, including but
not limited to compiled object code, generated documentation,
and conversions to other media types.

"Work" shall mean the work of authorship, whether in Source or
Object form, made available under the License, as indicated by a
copyright notice that is included in or attached to the work
(an example is provided in the Appendix below).

"Derivative Works" shall mean any work, whether in Source or Object
form, that is based on (or derived from) the Work and for which the
editorial revisions, annotations, elaborations, or other modifications
represent, as a whole, an original work of authorship. For the purposes
of this License, Derivative Works shall not include works that remain
separable from, or merely link (or bind by name) to the interfaces of,
the Work and Derivative Works thereof.

"Contribution" shall mean any work of authorship, including
the original version of the Work and any modifications or additions
to that Work or Derivative Works thereof, that is intentionally
submitted to Licensor for inclusion in the Work by the copyright owner
or by an individual or Legal Entity authorized to submit on behalf of
the copyright owner. For the purposes of this definition, "submitted"
means any form of electronic, verbal, or written communication sent
to the Licensor or its representatives, including but not limited to
communication on electronic mailing lists, source code control systems,
and issue tracking systems that are managed by, or on behalf of, the
Licensor for the purpose of discussing and improving the Work, but
excluding communication that is conspicuously marked or otherwise
designated in writing by the copyright owner as "Not a Contribution."

"Contributor" shall mean Licensor and any individual or Legal Entity
on behalf of whom a Contribution has been received by Licensor and
subsequently incorporated within the Work.

2. Grant of Copyright License. Subject to the terms and conditions of
this License, each Contributor hereby grants to You a perpetual,
worldwide, non-exclusive, no-charge, royalty-free, irrevocable
copyright license to reproduce, prepare Derivative Works of,
publicly display, publicly perform, sublicense, and distribute the
Work and such Derivative Works in Source or Object form.

3. Grant of Patent License. Subject to the terms and conditions of
this License, each Contributor hereby grants to You a perpetual,
worldwide, non-exclusive, no-charge, royalty-free, irrevocable
(except as stated in this section) patent license to make, have made,
use, offer to sell, sell, import, and otherwise transfer the Work,
where such license applies only to those patent claims licensable
by such Contributor that are necessarily infringed by their
Contribution(s) alone or by combination of their Contribution(s)
with the Work to which such Contribution(s) was submitted. If You
institute patent litigation against any entity (including a
cross-claim or counterclaim in a lawsuit) alleging that the Work
or a Contribution incorporated within the Work constitutes direct
or contributory patent infringement, then any patent licenses
granted to You under this License for that Work shall terminate
as of the date such litigation is filed.

4. Redistribution. You may reproduce and distribute copies of the
Work or Derivative Works thereof in any medium, with or without
modifications, and in Source or Object form, provided that You
meet the following conditions:

(a) You must give any other recipients of the Work or
Derivative Works a copy of this License; and

(b) You must cause any modified files to carry prominent notices
stating that You changed the files; and

(c) You must retain, in the Source form of any Derivative Works
that You distribute, all copyright, patent, trademark, and
attribution notices from the Source form of the Work,
excluding those notices that do not pertain to any part of
the Derivative Works; and

(d) If the Work includes a "NOTICE" text file as part of its
distribution, then any Derivative Works that You distribute must
include a readable copy of the attribution notices contained
within such NOTICE file, excluding those notices that do not
pertain to any part of the Derivative Works, in at least one
of the following places: within a NOTICE text file distributed
as part of the Derivative Works; within the Source form or
documentation, if provided along with the Derivative Works; or,
within a display generated by the Derivative Works, if and
wherever such third-party notices normally appear. The contents
of the NOTICE file are for informational purposes only and
do not modify the License. You may add Your own attribution
notices within Derivative Works that You distribute, alongside
or as an addendum to the NOTICE text from the Work, provided
that such additional attribution notices cannot be construed
as modifying the License.

You may add Your own copyright statement to Your modifications and
may provide additional or different license terms and conditions
for use, reproduction, or distribution of Your modifications, or
for any such Derivative Works as a whole, provided Your use,
reproduction, and distribution of the Work otherwise complies with
the conditions stated in this License.

5. Submission of Contributions. Unless You explicitly state otherwise,
any Contribution intentionally submitted for inclusion in the Work
by You to the Licensor shall be under the terms and conditions of
this License, without any additional terms or conditions.
Notwithstanding the above, nothing herein shall supersede or modify
the terms of any separate license agreement you may have executed
with Licensor regarding such Contributions.

6. Trademarks. This License does not grant permission to use the trade
names, trademarks, service marks, or product names of the Licensor,
except as required for reasonable and customary use in describing the
origin of the Work and reproducing the content of the NOTICE file.

7. Disclaimer of Warranty. Unless required by applicable law or
agreed to in writing, Licensor provides the Work (and each
Contributor provides its Contributions) on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or
implied, including, without limitation, any warranties or conditions
of TITLE, NON-INFRINGEMENT, MERCHANTABILITY, or FITNESS FOR A
PARTICULAR PURPOSE. You are solely responsible for determining the
appropriateness of using or redistributing the Work and assume any
risks associated with Your exercise of permissions under this License.

8. Limitation of Liability. In no event and under no legal theory,
whether in tort (including negligence), contract, or otherwise,
unless required by applicable law (such as deliberate and grossly
negligent acts) or agreed to in writing, shall any Contributor be
liable to You for damages, including any direct, indirect, special,
incidental, or consequential damages of any character arising as a
result of this License or out of the use or inability to use the
Work (including but not limited to damages for loss of goodwill,
work stoppage, computer failure or malfunction, or any and all
other commercial damages or losses), even if such Contributor
has been advised of the possibility of such damages.

9. Accepting Warranty or Additional Liability. While redistributing
the Work or Derivative Works thereof, You may choose to offer,
and charge a fee for, acceptance of support, warranty, indemnity,
or other liability obligations and/or rights consistent with this
License. However, in accepting such obligations, You may act only
on Your own behalf and on Your sole responsibility, not on behalf
of any other Contributor, and only if You agree to indemnify,
defend, and hold each Contributor harmless for any liability
incurred by, or claims asserted against, such Contributor by reason
of your accepting any such warranty or additional liability.

END OF TERMS AND CONDITIONS

Copyright 2017 Chikirev Sirguy, Unirail Group

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.**/

/**
in short
Permissions
	Commercial use
	Distribution
	Modification
	Private use
Conditions
	License and copyright notice
Limitations
	Liability
	Warranty
**/
﻿using System;
using System.Diagnostics;
using System.IO;

namespace org.unirail.BlackBox
{
    public abstract class Host
    {
        public abstract class Pack
        {
            public readonly Meta meta;
            public byte[] data;

            public Pack(Meta meta, int opt_bytes)
            {
                this.meta = meta;
                data = opt_bytes < 0 ? null : new byte[meta.packMinBytes + opt_bytes];
            }


            public abstract class Big : Pack
            {
                public byte[] bigdata;

                public Big(Meta meta, int bytes) : base(meta, 0)
                {
                    bigdata = data;
                    data = new byte[bytes];
                }
            }

            public class Meta
            {
                internal readonly int _2;
                internal readonly int _4;
                internal readonly int _8;













                internal readonly ushort _Pbbox;

                internal readonly int _ebbox;
                internal readonly Field[] fields;
                public readonly ushort id;

                internal readonly uint packMinBytes;

                public Meta(int id, int _2, int _4, int _8, int packMinBytes, int _ebbox) : this(id, _2, _4, _8, packMinBytes, _ebbox, 0, null) { }

                public Meta(int id, int _2, int _4, int _8, int packMinBytes, int _ebbox, int _Pbbox, params Field[] fields)
                {
                    this.id = (ushort) id;
                    this._2 = _2;
                    this._4 = _4;
                    this._8 = _8;
                    this._Pbbox = (ushort) _Pbbox;
                    this._ebbox = _ebbox;
                    this.fields = fields != null && fields.Length == 0 ? null : fields;
                    this.packMinBytes = (uint) packMinBytes;
                }

                public class Field
                {
                    internal readonly ushort type;

                    internal readonly bool varint;

                    internal readonly int length;
                    internal readonly sbyte size;

                    public readonly int[] dims;



                    internal readonly int _wbbox;


                    internal readonly char _Rbbox;
                    internal readonly char _bbbox;
                    internal readonly char _sbbox;

                    public Field(int type, bool varint, int length, int size, int _wbbox, int _Rbbox, int _sbbox, int _bbbox, params int[] dims)
                    {
                        this.type = (char) type;
                        this.varint = varint;
                        this.length = length;
                        this.size = (sbyte) size;
                        this._wbbox = _wbbox;
                        this._Rbbox = (char) _Rbbox;
                        this._bbbox = (char) _bbbox;
                        this._sbbox = (char) _sbbox;
                        this.dims = dims;
                    }


                    public class Bounds
                    {
                        public Pack pack;

                        public int field_bit;
                        public int field_size = 0;
                        internal Field field;
                        internal int _ybbox;


                        internal int _Zbbox = -1;

                        internal int _Dbbox = -1;
                        internal int _Bbbox = -1;

                        internal int _lbbox;
                        internal int _Cbbox;
                        internal int _cbbox;

                        public class Inside : Bounds
                        {

                            public int[] D = new int[10];

                            public int BIT = -1;
                            public int BYTE;

                            public int items;

                            internal int _nbbox;
                            internal int field_item;
                            internal int _Ebbox;
                            internal int _hbbox;

                            public bool wasNULL = false;

                            public override void setPack(Pack pack)
                            {
                                base.setPack(pack);
                                BIT = _ybbox;
                                BYTE = _Bbbox;
                                items = 0;
                                field_size = 0;
                                _nbbox = -1;
                                field_item = Int32.MaxValue;
                                _Ebbox = 0;
                            }
                        }

                        public virtual void setPack(Pack pack)
                        {
                            this.pack = pack;
                            field_bit = -1;
                            Meta meta = pack.meta;
                            _Dbbox = _Bbbox = (int)(meta.packMinBytes + (meta._Pbbox == 0 ? 0 : BitUtils.get_bits(pack.data, meta._ebbox - meta._Pbbox, meta._Pbbox)));
                            _ybbox = _Zbbox = (_Dbbox) << 3;
                            field_size = 0;
                            _cbbox = -1;
                            _lbbox = -1;
                            _Cbbox = -1;
                        }
                    }
                }
            }
        }


        public abstract class Channel : Stream
        {
            public override void Flush() { }
            public override long Seek(long offset, SeekOrigin origin) { throw new NotSupportedException(); }
            public override void SetLength(long value) { throw new NotSupportedException(); }
            public override bool CanSeek { get { return false; } }
            public override long Length { get { throw new NotSupportedException(); } }
            public override long Position { get { throw new NotSupportedException(); } set { throw new NotSupportedException(); } }

            protected internal static byte pack_id_bytes = 1;

            private readonly Flow b2p = new Flow();
            private readonly Flow p2b = new Flow();

            public abstract class Advanced : Channel
            {
                private static readonly ushort[] tab = {0, 4129, 8258, 12387, 16516, 20645, 24774, 28903, 33032, 37161, 41290, 45419, 49548, 53677, 57806, 61935};


                private static ushort crc16(byte data, ushort crc)
                {
                    crc = (ushort)(tab[(crc >> 12 ^ data >> 4) & 0x0F] ^ crc << 4);
                    return (ushort)(tab[(crc >> 12 ^ data & 0x0F) & 0x0F] ^ crc << 4);
                }

                internal const byte BR = 0x55;

                public override void Write(byte[] src, int BYTE, int bytes)
                {
                    time = ~0;
                    if(time == 0)
                    {
                        time = ~0;
                        b2p.state = Flow.STATE.STANDBY;
                        if(b2p.pack != null) b2p.pack = null;
                    }
                    for(int t; 0 < bytes--; t = b2p.mode != Flow.MODE.CRC ? b2p.crc = crc16(src[BYTE], b2p.crc) : 0, BYTE++)
                    {
                        switch(b2p.state)
                        {
                            case Flow.STATE.STANDBY:
                                b2p.crc = 0;
                                b2p._gbbox = 0;
                                bits = 0;
                                if(src[BYTE] == BR) b2p.state = Flow.STATE.PACK_ID;
                                continue;
                            case Flow.STATE.PACK_ID:
                                if(src[BYTE] == BR)
                                {
                                    failure(" After BR expect helper ID but got +BR");
                                    b2p.state = Flow.STATE.STANDBY;
                                    continue;
                                }
                                b2p._gbbox = (b2p._gbbox << 8) | src[BYTE];
                                if(++bits < pack_id_bytes) continue;
                                b2p.mode = Flow.MODE.NONE;
                                Debug.Assert(b2p.pack == null);
                                if((b2p.pack = process(null, (int) b2p._gbbox)) == null)
                                {
                                    failure("Unrecognized package ID = " + b2p._gbbox);
                                    b2p.state = Flow.STATE.STANDBY;
                                    continue;
                                }
                                b2p._gbbox = 0;
                                bits = 0;
                                b2p.field_bit = -1;
                                b2p._ybbox = b2p._Zbbox = b2p._Dbbox = b2p._Bbbox = b2p.field_size = 0;
                                if(b2p.pack.meta.fields != null)
                                {
                                    b2p.state = Flow.STATE.VARINT;
                                    b2p.mode = Flow.MODE.OPTS_INFO;
                                    continue;
                                }
                                break;
                            case Flow.STATE.BYTES:
                                if(src[BYTE] == BR)
                                {
                                    b2p.state = Flow.STATE.BYTES_BR;
                                    continue;
                                }
                            goto case Flow.STATE.BYTES_BR;
                            case Flow.STATE.BYTES_BR:
                                if(b2p.state == Flow.STATE.BYTES_BR)
                                {
                                    if(src[BYTE] != BR)
                                    {
                                        failure("waiting for second BR but got " + src[BYTE]);
                                        b2p.state = Flow.STATE.STANDBY;
                                        continue;
                                    }
                                    b2p.state = Flow.STATE.BYTES;
                                }
                                if(b2p.mode == Flow.MODE.CRC)
                                    switch(b2p.field_size)
                                    {
                                        case 2:
                                            b2p._gbbox = (ulong)(src[BYTE] << 8);
                                            b2p.field_size = 1;
                                            continue;
                                        case 1:
                                            if((b2p._gbbox | src[BYTE]) == b2p.crc) process(b2p.pack, PROCESS_CHANNEL_REQEST);
                                            else failure("CRC error");
                                            b2p.state = Flow.STATE.STANDBY;
                                            b2p.mode = Flow.MODE.NONE;
                                            b2p.pack = null;
                                            continue;
                                    }
                                b2p.pack.data[b2p._Bbbox++] = src[BYTE];
                                if(b2p._Bbbox < b2p._Dbbox) continue;
                                break;
                            case Flow.STATE.VARINT:
                                if(src[BYTE] == BR)
                                {
                                    b2p.state = Flow.STATE.VARINT_BR;
                                    continue;
                                }
                            goto case Flow.STATE.VARINT_BR;
                            case Flow.STATE.VARINT_BR:
                                if(b2p.state == Flow.STATE.VARINT_BR)
                                {
                                    if(src[BYTE] != BR)
                                    {
                                        failure("waiting for second BR but got " + src[BYTE]);
                                        b2p.state = Flow.STATE.STANDBY;
                                        continue;
                                    }
                                    b2p.state = Flow.STATE.VARINT;
                                }
                                b2p._gbbox |= (src[BYTE] & 0x7FUL) << bits;
                                bits += 7;
                                if((src[BYTE] & 0x80) != 0) continue;
                                bits = 0;
                                if(b2p.mode == Flow.MODE.OPTS_INFO)
                                {
                                    b2p.pack.data = new byte[b2p.pack.meta.packMinBytes + (int) b2p._gbbox];
                                    b2p._gbbox = 0;
                                    b2p.mode = Flow.MODE.NONE;
                                    break;
                                }
                                BitUtils.set_bytes(b2p._gbbox, b2p.field_size, b2p.pack.data, b2p._Bbbox);
                                b2p._gbbox = 0;
                                if((b2p._Bbbox += b2p.field_size) < b2p._Dbbox) continue;
                                break;
                        }
                        if(b2p.next()) continue;
                        b2p.state = Flow.STATE.BYTES;
                        b2p.mode = Flow.MODE.CRC;
                        b2p.field_size = 2;
                    }
                }

                public override int Read(byte[] dst, int BYTE, int bytes)
                {
                    int fix = BYTE;
                    for(int t; 0 < bytes--; t = p2b.mode != Flow.MODE.CRC ? p2b.crc = crc16(dst[BYTE], p2b.crc) : 0, BYTE++)
                    {
                        switch(p2b.state)
                        {
                            case Flow.STATE.STANDBY:
                            {
                                if((p2b.pack = process(null, PROCESS_CHANNEL_REQEST)) == null) return BYTE - fix;
                                p2b.state = Flow.STATE.PACK_ID;
                                p2b._gbbox = p2b.pack.meta.id;
                                p2b.field_bit = 8 * (pack_id_bytes - 1);
                                p2b.crc = 0;
                                dst[BYTE] = BR;
                                continue;
                            }
                            case Flow.STATE.PACK_ID:
                                dst[BYTE] = (byte)(p2b._gbbox >> p2b.field_bit);
                                if(-1 < (p2b.field_bit -= 8)) continue;
                                p2b._gbbox = 0;
                                p2b.field_bit = -1;
                                p2b._ybbox = p2b._Zbbox = p2b._Dbbox = p2b._Bbbox = p2b.field_size = 0;
                                p2b.mode = Flow.MODE.NONE;
                                if(p2b.pack.meta.fields != null)
                                {
                                    p2b.state = Flow.STATE.VARINT;
                                    Pack.Meta meta = p2b.pack.meta;
                                    byte[] data = p2b.pack.data;
                                    p2b.setPack(p2b.pack);
                                    _Ubbox(p2b);
                                    p2b._gbbox = (ulong)(p2b._lbbox + 1 - meta.packMinBytes);
                                    p2b._ybbox = p2b._Zbbox = p2b._Dbbox = p2b._Bbbox = p2b.field_size = 0;
                                    continue;
                                }
                                break;
                            case Flow.STATE.BYTES:
                                if(p2b.mode == Flow.MODE.CRC)
                                {
                                    switch(p2b.field_size)
                                    {
                                        case 4:
                                            p2b.field_size = (dst[BYTE] = (byte)(p2b.crc >> 8)) == BR ? 3 : 2;
                                            continue;
                                        case 3:
                                            dst[BYTE] = BR;
                                            p2b.field_size = 2;
                                            continue;
                                        case 2:
                                            if((dst[BYTE] = (byte)(p2b.crc & 0xFF)) != BR) break;
                                            p2b.field_size = 1;
                                            continue;
                                        case 1:
                                            dst[BYTE] = BR;
                                            break;
                                    }
                                    p2b.state = Flow.STATE.STANDBY;
                                    p2b.mode = Flow.MODE.NONE;
                                    p2b.pack = null;
                                    continue;
                                }
                                if((p2b._gbbox = dst[BYTE] = p2b.pack.data[p2b._Bbbox++]) == BR)
                                {
                                    p2b.state = Flow.STATE.BYTES_BR;
                                    continue;
                                }
                            goto case Flow.STATE.BYTES_BR;
                            case Flow.STATE.BYTES_BR:
                                p2b.state = Flow.STATE.BYTES;
                                dst[BYTE] = (byte) p2b._gbbox;
                                if(p2b._Bbbox < p2b._Dbbox) continue;
                                break;
                            case Flow.STATE.VARINT:
                                if((p2b._gbbox & ~0x7FUL) != 0)
                                {
                                    dst[BYTE] = (byte)(p2b._gbbox & 0x7F | 0x80);
                                    p2b._gbbox >>= 7;
                                    continue;
                                }
                                if(p2b._gbbox == BR)
                                {
                                    p2b.state = Flow.STATE.VARINT_BR;
                                    dst[BYTE] = BR;
                                    continue;
                                }
                            goto case Flow.STATE.VARINT_BR;
                            case Flow.STATE.VARINT_BR:
                                p2b.state = Flow.STATE.VARINT;
                                dst[BYTE] = (byte) p2b._gbbox;
                                if((p2b._Bbbox += p2b.field_size) < p2b._Dbbox)
                                {
                                    p2b._gbbox = BitUtils.get_bytes(p2b.pack.data, p2b._Bbbox, p2b.field_size);
                                    continue;
                                }
                                break;
                        }
                        if(p2b.next())
                        {
                            if(p2b.state == Flow.STATE.VARINT) p2b._gbbox = BitUtils.get_bytes(p2b.pack.data, p2b._Bbbox, p2b.field_size);
                            continue;
                        }
                        p2b.state = Flow.STATE.BYTES;
                        p2b.mode = Flow.MODE.CRC;
                        p2b.field_size = 4;
                    }
                    return BYTE - fix;
                }
            }

            private int bits;

            private volatile int time;

            public virtual void failure(String reasone) { Debug.Print("failure: " + reasone); }






            internal const int PROCESS_CHANNEL_REQEST = -1, PROCESS_HOST_REQEST = -2, PROCESS_RECEIVED = -3;

            protected internal abstract Pack process(Pack pack, int id);

            public override void Write(byte[] src, int BYTE, int bytes)
            {
                time = ~0;
                if(time == 0)
                {
                    time = ~0;
                    b2p.state = Flow.STATE.STANDBY;
                    if(b2p.pack != null) b2p.pack = null;
                }
                for(; 0 < bytes--; BYTE++)
                {
                    switch(b2p.state)
                    {
                        case Flow.STATE.STANDBY:
                            b2p._gbbox = 0;
                            bits = 0;
                            b2p.state = Flow.STATE.PACK_ID;
                        goto case Flow.STATE.PACK_ID;
                        case Flow.STATE.PACK_ID:
                            b2p._gbbox = (b2p._gbbox << 8) | src[BYTE];
                            if(++bits < pack_id_bytes) continue;
                            b2p.mode = Flow.MODE.NONE;
                            Debug.Assert(b2p.pack == null);
                            if((b2p.pack = process(null, (int) b2p._gbbox)) == null)
                            {
                                failure("Unrecognized package ID = " + b2p._gbbox);
                                b2p.state = Flow.STATE.STANDBY;
                                continue;
                            }
                            b2p._gbbox = 0;
                            bits = 0;
                            b2p.field_bit = -1;
                            b2p._ybbox = b2p._Zbbox = b2p._Dbbox = b2p._Bbbox = b2p.field_size = 0;
                            if(b2p.pack.meta.fields != null)
                            {
                                b2p.state = Flow.STATE.VARINT;
                                b2p.mode = Flow.MODE.OPTS_INFO;
                                continue;
                            }
                            break;
                        case Flow.STATE.BYTES:
                            b2p.pack.data[b2p._Bbbox++] = src[BYTE];
                            if(b2p._Bbbox < b2p._Dbbox) continue;
                            break;
                        case Flow.STATE.VARINT:
                            b2p._gbbox |= (src[BYTE] & 0x7FUL) << bits;
                            bits += 7;
                            if((src[BYTE] & 0x80) != 0) continue;
                            bits = 0;
                            if(b2p.mode == Flow.MODE.OPTS_INFO)
                            {
                                b2p.pack.data = new byte[b2p.pack.meta.packMinBytes + (int) b2p._gbbox];
                                b2p._gbbox = 0;
                                b2p.mode = Flow.MODE.NONE;
                                break;
                            }
                            BitUtils.set_bytes(b2p._gbbox, b2p.field_size, b2p.pack.data, b2p._Bbbox);
                            b2p._gbbox = 0;
                            if((b2p._Bbbox += b2p.field_size) < b2p._Dbbox) continue;
                            break;
                    }
                    if(b2p.next()) continue;
                    b2p.state = Flow.STATE.STANDBY;
                    b2p.mode = Flow.MODE.NONE;
                    if(b2p.pack != null)
                    {
                        process(b2p.pack, PROCESS_CHANNEL_REQEST);
                        b2p.pack = null;
                    }
                }
            }

            public override int Read(byte[] dst, int BYTE, int bytes)
            {
                int fix = BYTE;
                for(; 0 < bytes--; BYTE++)
                {
                    switch(p2b.state)
                    {
                        case Flow.STATE.STANDBY:
                        {
                            if((p2b.pack = process(null, PROCESS_CHANNEL_REQEST)) == null) return BYTE - fix;
                            p2b.state = Flow.STATE.PACK_ID;
                            p2b._gbbox = p2b.pack.meta.id;
                            p2b.field_bit = 8 * (pack_id_bytes - 1);
                        }
                        goto case Flow.STATE.PACK_ID;
                        case Flow.STATE.PACK_ID:
                            dst[BYTE] = (byte)(p2b._gbbox >> p2b.field_bit);
                            if(-1 < (p2b.field_bit -= 8)) continue;
                            p2b._gbbox = 0;
                            p2b.field_bit = -1;
                            p2b._ybbox = p2b._Zbbox = p2b._Dbbox = p2b._Bbbox = p2b.field_size = 0;
                            p2b.mode = Flow.MODE.NONE;
                            if(p2b.pack.meta.fields != null)
                            {
                                p2b.state = Flow.STATE.VARINT;
                                Pack.Meta meta = p2b.pack.meta;
                                p2b.setPack(p2b.pack);
                                _Ubbox(p2b);
                                p2b._gbbox = (ulong)(p2b._lbbox + 1 - meta.packMinBytes);
                                p2b._ybbox = p2b._Zbbox = p2b._Dbbox = p2b._Bbbox = p2b.field_size = 0;
                                continue;
                            }
                            break;
                        case Flow.STATE.BYTES:
                            dst[BYTE] = p2b.pack.data[p2b._Bbbox++];
                            if(p2b._Bbbox < p2b._Dbbox) continue;
                            break;
                        case Flow.STATE.VARINT:
                            if((p2b._gbbox & ~0x7FUL) != 0)
                            {
                                dst[BYTE] = (byte)(p2b._gbbox & 0x7F | 0x80);
                                p2b._gbbox >>= 7;
                                continue;
                            }
                            dst[BYTE] = (byte) p2b._gbbox;
                            if((p2b._Bbbox += p2b.field_size) < p2b._Dbbox)
                            {
                                p2b._gbbox = BitUtils.get_bytes(p2b.pack.data, p2b._Bbbox, p2b.field_size);
                                continue;
                            }
                            break;
                    }
                    if(p2b.next())
                    {
                        if(p2b.state == Flow.STATE.VARINT) p2b._gbbox = BitUtils.get_bytes(p2b.pack.data, p2b._Bbbox, p2b.field_size);
                        continue;
                    }
                    p2b.state = Flow.STATE.STANDBY;
                    p2b.mode = Flow.MODE.NONE;
                    p2b.pack = null;
                }
                return BYTE - fix;
            }

            internal class Flow : Pack.Meta.Field.Bounds
            {
                internal ushort crc;

                internal MODE mode = MODE.NONE;

                internal STATE state = STATE.STANDBY;

                internal ulong _gbbox;

                internal virtual bool next()
                {
                    var meta = pack.meta;
                    var data = pack.data;
                    int delta;
                    if(field_bit == -1)
                        switch(field_size)
                        {
                            case 0:
                                if(0 < meta._2)
                                {
                                    _Dbbox += (int) meta._2 * (field_size = 2);
                                    state = STATE.VARINT;
                                    return true;
                                }
                            goto case 2;
                            case 2:
                                if(0 < meta._4)
                                {
                                    _Dbbox += (int) meta._4 * (field_size = 4);
                                    state = STATE.VARINT;
                                    return true;
                                }
                            goto case 4;
                            case 4:
                                if(0 < meta._8)
                                {
                                    _Dbbox += (int) meta._8 * (field_size = 8);
                                    state = STATE.VARINT;
                                    return true;
                                }
                            goto case 8;
                            case 8:
                                if(_Bbbox < (_Dbbox = (int) meta.packMinBytes))
                                {
                                    state = STATE.BYTES;
                                    field_size = 1;
                                    return true;
                                }
                                mode = MODE.NONE;
                                goto default;
                            default:
                                if(meta.fields == null)
                                    return false;
                                int fix = _Bbbox;
                                setPack(pack);
                                if(mode == MODE.NONE && fix < _Dbbox)
                                {
                                    _Bbbox = fix;
                                    state = STATE.BYTES;
                                    mode = MODE.OPTS_INFO;
                                    field_size = 1;
                                    return true;
                                }
                                mode = MODE.NONE;
                                if((delta = BitUtils.first_1(data, (int) meta._ebbox, (int)(_ybbox - meta._ebbox), false)) == -1) return false;
                                field_bit = (int) meta._ebbox - 1;
                                break;
                        }
                    else
                    {
                        if(field_bit - meta._ebbox == meta.fields.Length - 1 || (delta = BitUtils.first_1(data, field_bit + 1, _ybbox - (field_bit + 1), false)) == -1) return false;
                    }
                    do
                    {
                        field_bit += delta + 1;
                        var fld = pack.meta.fields[field_bit - meta._ebbox];
                        state = fld.varint ? STATE.VARINT : STATE.BYTES;
                        _Zbbox = _ybbox;
                        _Bbbox = _Dbbox;
                        if(0 < fld.length)
                            if(0 < fld.size)
                                _Dbbox += fld._wbbox * fld.length * (field_size = fld.size);
                            else
                                _ybbox += fld._wbbox * fld.length * fld.size;
                        else setBounds(fld, this);
                        if(_Bbbox == _Dbbox)
                            if((delta = BitUtils.first_1(data, field_bit + 1, _ybbox - (field_bit + 1), false)) == -1)
                                return false;
                            else
                                continue;
                        break;
                    }
                    while(true);
                    return true;
                }

                internal enum STATE
                {
                    STANDBY,
                    PACK_ID,
                    VARINT,
                    VARINT_BR,
                    BYTES,
                    BYTES_BR
                }

                internal enum MODE
                {
                    PACKK_ID,
                    OPTS_INFO,
                    SET,
                    NONE,
                    CRC
                }
            }
        }



        static void init(Pack.Meta.Field fld, Pack.Meta.Field.Bounds.Inside ph)
        {
            switch(fld.type)
            {
                case 1:
                    ph.items = -fld.length;
                    ph.BIT = ph._ybbox;
                    ph.BYTE = ph._Bbbox;
                    break;
                case 2:
                case 6:
                case 8:
                case 12:
                    ph.items = -fld.length;
                    ph.BYTE = ph._Bbbox;
                    ph.BIT = ph._Zbbox - fld._Rbbox;
                    ph.field_item = Int32.MaxValue;
                    ph._Ebbox = (int) BitUtils.get_bits(ph.pack.data, ph.BIT, fld._sbbox);
                    ph._nbbox = (int) BitUtils.get_bits(ph.pack.data, ph.BIT + fld._sbbox, fld._sbbox);
                    break;
                case 3:
                    ph.BYTE = ph._Bbbox;
                    ph.items = (int) BitUtils.get_bits(ph.pack.data, (ph.BIT = ph._Zbbox - fld._Rbbox), -fld.length);
                    break;
                case 4:
                    ph.items = (int) BitUtils.get_bits(ph.pack.data, (ph.BIT = ph._Zbbox - fld._Rbbox) + 2 * fld._sbbox, -fld.length);
                    ph.BYTE = ph._Bbbox;
                    ph.field_item = Int32.MaxValue;
                    ph._Ebbox = (int) BitUtils.get_bits(ph.pack.data, ph.BIT, fld._sbbox);
                    ph._nbbox = (int) BitUtils.get_bits(ph.pack.data, ph.BIT + fld._sbbox, fld._sbbox);
                    break;
                case 5:
                case 11:
                    ph.items = 0;
                    ph.BYTE = ph._Bbbox;
                    ph.BIT = ph._Zbbox - fld._Rbbox;
                    ph._nbbox = 0;
                    ph.field_item = Int32.MaxValue;
                    ph._Ebbox = ph._hbbox;
                    break;
                case 7:
                    ph.items = -fld.length;
                    ph.BYTE = ph._Bbbox;
                    ph._nbbox = 0;
                    ph.BIT = ph._ybbox;
                    ph.field_item = (ph._Ebbox = ph._hbbox) == 0 ? Int32.MaxValue : 0;
                    break;
                case 9:
                    ph.items = (int) BitUtils.get_bits(ph.pack.data, (ph.BIT = ph._Zbbox - fld._Rbbox), -fld.length);
                    ph.BYTE = ph._Bbbox;
                    ph._nbbox = 0;
                    ph.BIT = ph._ybbox;
                    ph.field_item = (ph._Ebbox = ph._hbbox) == 0 ? Int32.MaxValue : 0;
                    break;
                case 10:
                    ph.items = (int) BitUtils.get_bits(ph.pack.data, (ph.BIT = ph._Zbbox - fld._Rbbox) + 2 * fld._sbbox, -fld.length);
                    ph.BYTE = ph._Bbbox;
                    ph.field_item = Int32.MaxValue;
                    ph._Ebbox = (int) BitUtils.get_bits(ph.pack.data, ph.BIT, fld._sbbox);
                    ph._nbbox = (int) BitUtils.get_bits(ph.pack.data, ph.BIT + fld._sbbox, fld._sbbox);
                    break;
                default:
                    Debug.Assert(false);
                    return;
            }
        }

        internal static void setBounds(Pack.Meta.Field fld, Pack.Meta.Field.Bounds ph)
        {
            ph.field_size = fld.size;
            switch(fld.type)
            {
                case 1:
                    int count = 1;
                    if(0 < fld._bbbox)
                        for(int i = 0, _bit = ph._ybbox; i < fld.dims.Length; i++)
                            if(fld.dims[i] < 0)
                                count *= (int) BitUtils.get_bits(ph.pack.data, _bit -= -fld.dims[i], -fld.dims[i]);
                    ph._ybbox -= fld._Rbbox;
                    ph._Dbbox += fld._wbbox * count * -fld.length * fld.size;
                    break;
                case 2:
                    count = (int) BitUtils.get_bits(ph.pack.data, ph._ybbox -= fld._Rbbox, fld._sbbox);
                    if(count == 0) return;
                    ph.field_size = fld.size;
                    int bit = ph._ybbox;
                    ph._ybbox -= count;
                    byte[] data = ph.pack.data;
                    while(ph._ybbox < bit--)
                        if((data[bit >> 3] & 1 << (bit & 7)) == 0)
                            count--;
                    ph._Dbbox += -fld.length * count * fld.size;
                    break;
                case 3:
                    ph.field_size = fld.size;
                    bit = ph._ybbox;
                    ph._ybbox -= fld._Rbbox;
                    int arrays_length = (int) BitUtils.get_bits(ph.pack.data, ph._ybbox, -fld.length);
                    if(0 < fld._bbbox)
                        for(int i = 0; i < fld.dims.Length; i++)
                            if(fld.dims[i] < 0)
                                arrays_length *= (int) BitUtils.get_bits(ph.pack.data, bit -= -fld.dims[i], -fld.dims[i]);
                    ph._Dbbox += fld._wbbox * arrays_length * fld.size;
                    break;
                case 4:
                    count = (int) BitUtils.get_bits(ph.pack.data, ph._ybbox -= fld._Rbbox, fld._sbbox);
                    if(count == 0) return;
                    ph.field_size = fld.size;
                    bit = ph._ybbox;
                    ph._ybbox -= count;
                    data = ph.pack.data;
                    arrays_length = (int) BitUtils.get_bits(data, bit + 2 * fld._sbbox, -fld.length);
                    while(ph._ybbox < bit--)
                        if((data[bit >> 3] & 1 << (bit & 7)) == 0)
                            count--;
                    ph._Dbbox += count * arrays_length * fld.size;
                    break;
                case 5:
                    ph.field_size = fld.size;
                    count = fld._wbbox;
                    if(0 < fld._bbbox)
                        for(int i = 0, _bit = ph._ybbox; i < fld.dims.Length; i++)
                            if(fld.dims[i] < 0)
                                count *= (int) BitUtils.get_bits(ph.pack.data, _bit -= -fld.dims[i], -fld.dims[i]);
                    ph._ybbox -= fld._Rbbox;
                    int all_arrays_sum = 0;
                    while(0 < count--) all_arrays_sum += (int) BitUtils.get_bits(ph.pack.data, ph._ybbox -= -fld.length, -fld.length);
                    ph._Dbbox += all_arrays_sum * fld.size;
                    break;
                case 6:
                    count = (int) BitUtils.get_bits(ph.pack.data, ph._ybbox -= fld._Rbbox, fld._sbbox);
                    if(count == 0) return;
                    ph.field_size = fld.size;
                    bit = ph._ybbox;
                    all_arrays_sum = 0;
                    data = ph.pack.data;
                    while(0 < count--)
                        if((data[--bit >> 3] & 1 << (bit & 7)) != 0)
                            all_arrays_sum += (int) BitUtils.get_bits(ph.pack.data, bit -= -fld.length, -fld.length);
                    ph._ybbox = bit;
                    ph._Dbbox += all_arrays_sum * fld.size;
                    break;
                case 7:
                    ph.field_size = 0;
                    count = 1;
                    if(0 < fld._bbbox)
                        for(int i = 0, _bit = ph._ybbox; i < fld.dims.Length; i++)
                            if(fld.dims[i] < 0)
                                count *= (int) BitUtils.get_bits(ph.pack.data, _bit -= -fld.dims[i], -fld.dims[i]);
                    ph._ybbox -= fld._Rbbox + count * fld._wbbox * -fld.length * fld.size;
                    break;
                case 8:
                    count = (int) BitUtils.get_bits(ph.pack.data, ph._ybbox -= fld._Rbbox, fld._sbbox);
                    if(count == 0) return;
                    bit = ph._ybbox;
                    ph.field_size = fld.size;
                    data = ph.pack.data;
                    for(int bits = -fld.length * fld.size; 0 < count--;)
                        if((data[--bit >> 3] & 1 << (bit & 7)) != 0)
                            bit -= bits;
                    ph._ybbox = bit;
                    break;
                case 9:
                    ph.field_size = fld.size;
                    bit = ph._ybbox;
                    ph._ybbox -= fld._Rbbox;
                    arrays_length = (int) BitUtils.get_bits(ph.pack.data, ph._ybbox, -fld.length);
                    if(0 < fld._bbbox)
                        for(int i = 0; i < fld.dims.Length; i++)
                            if(fld.dims[i] < 0)
                                arrays_length *= (int) BitUtils.get_bits(ph.pack.data, bit -= -fld.dims[i], -fld.dims[i]);
                    ph._ybbox -= fld._wbbox * arrays_length * fld.size;
                    break;
                case 10:
                    count = (int) BitUtils.get_bits(ph.pack.data, ph._ybbox -= fld._Rbbox, fld._sbbox);
                    if(count == 0) return;
                    bit = ph._ybbox;
                    ph.field_size = fld.size;
                    data = ph.pack.data;
                    for(int bits = (int) BitUtils.get_bits(data, bit + 2 * fld._sbbox, -fld.length) * fld.size; 0 < count--;)
                        if((data[--bit >> 3] & 1 << (bit & 7)) != 0)
                            bit -= bits;
                    ph._ybbox = bit;
                    break;
                case 11:
                    ph.field_size = fld.size;
                    count = fld._wbbox;
                    if(0 < fld._bbbox)
                        for(int i = 0, _bit = ph._ybbox; i < fld.dims.Length; i++)
                            if(fld.dims[i] < 0)
                                count *= (int) BitUtils.get_bits(ph.pack.data, _bit -= -fld.dims[i], -fld.dims[i]);
                    ph._ybbox -= fld._Rbbox;
                    while(0 < count--)
                    {
                        int bits = (int) BitUtils.get_bits(ph.pack.data, ph._ybbox -= -fld.length, -fld.length) * fld.size;
                        ph._ybbox -= bits;
                    }
                    break;
                case 12:
                    count = (int) BitUtils.get_bits(ph.pack.data, ph._ybbox -= fld._Rbbox, fld._sbbox);
                    if(count == 0) return;
                    ph.field_size = fld.size;
                    bit = ph._ybbox;
                    all_arrays_sum = 0;
                    data = ph.pack.data;
                    while(0 < count--)
                        if((data[--bit >> 3] & 1 << (bit & 7)) != 0)
                        {
                            int len = (int) BitUtils.get_bits(ph.pack.data, bit -= -fld.length, -fld.length);
                            bit -= len * fld.size;
                        }
                    ph._ybbox = bit;
                    break;
                default:
                    Debug.Assert(false);
                    return;
            }
        }

        static void insert(Pack.Meta.Field.Bounds.Inside ph, int fbit, int bits, int bytes)
        {
            if(ph.field_bit != fbit)
            {
                ph.BIT = ph._Zbbox = ph._ybbox;
                ph.BYTE = ph._Bbbox = ph._Dbbox;
            }
            Pack.Meta meta = ph.pack.meta;
            if(ph._lbbox < 0) _Ubbox(ph);
            int add_to_bits_bytes;
            if(ph.BIT == ph._cbbox && ph.BYTE == ph._lbbox)
            {
                add_to_bits_bytes = meta._Pbbox == 0 ? 0 : BitUtils.bits2bytes(fbit + bits - ph._cbbox + 1);
                if(0 < add_to_bits_bytes || 0 < bytes)
                {
                    int old_pack_data_bytes = ph._lbbox;
                    Debug.Assert(old_pack_data_bytes == ph.pack.data.Length);
                    byte[] data = new byte[old_pack_data_bytes + add_to_bits_bytes + bytes];
                    if(ph.field_bit == -1 || bits == 0)
                        Array.Copy(ph.pack.data, 0, data, 0, old_pack_data_bytes);
                    else
                    {
                        BitUtils.copy_bits(ph.pack.data, 0,
                                           ph.field_bit + 1,
                                           data, 0);
                        BitUtils.copy_bits(ph.pack.data, ph.BIT,
                                           (old_pack_data_bytes << 3) - ph.BIT, data, ph.BIT + (add_to_bits_bytes << 3));
                    }
                    ph._lbbox += add_to_bits_bytes + bytes;
                    ph.pack.data = data;
                }
            }
            else
            {
                int add_bits_bits = bits - (ph._cbbox - (ph._Cbbox + 1));
                add_to_bits_bytes = BitUtils.bits2bytes(add_bits_bits);
                if(0 < add_to_bits_bytes || 0 < bytes)
                {
                    int old_pack_data_bytes = ph._lbbox;
                    Debug.Assert(old_pack_data_bytes == ph.pack.data.Length);
                    byte[] data = new byte[old_pack_data_bytes + add_to_bits_bytes + bytes];
                    if(bits == 0)
                        Array.Copy(ph.pack.data, 0, data, 0, ph.BYTE + 1);
                    else
                    {
                        BitUtils.copy_bits(ph.pack.data, 0,
                                           ph._Cbbox + 1,
                                           data, 0);
                        BitUtils.copy_bits(ph.pack.data, ph._cbbox,
                                           ph.BIT - ph._cbbox, data, ph._cbbox + (add_to_bits_bytes << 3) - bits);
                        BitUtils.copy_bits(ph.pack.data, ph.BIT, (ph.BYTE << 3) - ph.BIT, data, ph.BIT + (add_to_bits_bytes << 3));
                    }
                    Array.Copy(
                        ph.pack.data, ph.BYTE, data, ph.BYTE + add_to_bits_bytes + bytes, ph._lbbox - ph.BYTE);
                    ph._lbbox += add_to_bits_bytes + bytes;
                    ph.pack.data = data;
                }
                else
                {
                    BitUtils.copy_bits(ph.pack.data, ph._cbbox,
                                       ph.BIT - ph._cbbox, ph.pack.data, ph._cbbox - bits);
                    BitUtils.set_0(ph.pack.data, ph.BIT - bits, bits);
                }
            }
            if(0 < meta._Pbbox && 0 < add_to_bits_bytes)
            {
                int old_value = (int) BitUtils.get_bits(ph.pack.data, meta._ebbox - meta._Pbbox, meta._Pbbox);
                BitUtils.set_bits((uint)(old_value + add_to_bits_bytes), meta._Pbbox, ph.pack.data, meta._ebbox - meta._Pbbox);
            }
            int zazor_delta = (add_to_bits_bytes << 3) - bits;
            ph._cbbox += zazor_delta;
            ph.BIT += add_to_bits_bytes << 3;
            ph.BYTE += add_to_bits_bytes;
            if(fbit == ph.field_bit)
            {
                ph._ybbox += zazor_delta;
                ph._Zbbox += add_to_bits_bytes << 3;
                ph._Bbbox += add_to_bits_bytes;
                ph._Dbbox += add_to_bits_bytes + bytes;
            }
            else
            {
                ph._Zbbox = ph._ybbox = ph.BIT;
                ph._Dbbox = ph._Bbbox = ph.BYTE;
                ph.pack.data[fbit >> 3] |= (byte)(1 << (fbit & 7));
                if(ph._Cbbox < fbit) ph._Cbbox = fbit;
                ph.field_bit = fbit;
                ph.field = meta.fields[ph.field_bit - meta._ebbox];
            }
        }

        static bool visit_field(Pack.Meta.Field.Bounds.Inside ph, int fbit)
        {
            if(ph.field_bit == fbit) return true;
            Pack.Meta meta = ph.pack.meta;
            byte[] data = ph.pack.data;
            int delta;
            for(;;)
                if(ph.field_bit == -1)
                {
                    if((delta = BitUtils.first_1(data, meta._ebbox, ph._ybbox - meta._ebbox, false)) == -1 ||
                            fbit < meta._ebbox + delta) return false;
                    ph.field_bit = meta._ebbox - 1;
                    break;
                }
                else if(fbit < ph.field_bit)
                    ph.setPack(ph.pack);
                else
                {
                    if(ph.field_bit - meta._ebbox == meta.fields.Length - 1 ||
                            (delta = BitUtils.first_1(data, ph.field_bit + 1, ph._ybbox - (ph.field_bit + 1), false)) == -1 ||
                            fbit < ph.field_bit + 1 + delta) return false;
                    break;
                }
            for(;;)
            {
                Pack.Meta.Field fld = meta.fields[(ph.field_bit += 1 + delta) - meta._ebbox];
                ph.field = fld;
                ph._Zbbox = ph._ybbox;
                ph._Bbbox = ph._Dbbox;
                if(0 < fld.length)
                    if(0 < fld.size)
                        ph._Dbbox += fld._wbbox * fld.length * fld.size;
                    else
                        ph._ybbox += fld._wbbox * fld.length * fld.size;
                else setBounds(fld, ph);
                if(ph.field_bit == fbit || ph.field_bit - meta._ebbox == meta.fields.Length - 1 ||
                        (delta = BitUtils.first_1(data, ph.field_bit + 1, ph._ybbox - (ph.field_bit + 1), false)) == -1 ||
                        fbit < ph.field_bit + 1 + delta)
                {
                    ph.items = fld.length;
                    ph.field_size = fld.size;
                    ph.BIT = ph._ybbox;
                    ph.BYTE = ph._Bbbox;
                    if(fld.length < 1)
                    {
                        ph._hbbox = fld._wbbox;
                        if(0 < fld._bbbox)
                        {
                            if(ph.D == null || ph.D.Length < fld.dims.Length) ph.D = new int[fld.dims.Length];
                            else
                                for(int i = ph.D.Length - 1; -1 < i;)
                                    ph.D[i--] = 0;
                            for(int i = 0, bit = ph._Zbbox, dim; i < fld.dims.Length; i++)
                                if((dim = fld.dims[i]) < 1)
                                    ph._hbbox *= ph.D[i] = (int) BitUtils.get_bits(data, bit -= -dim, -dim);
                                else
                                    ph.D[i] = dim;
                        }
                        init(fld, ph);
                    }
                    return ph.field_bit == fbit;
                }
            }
        }

        static void _Ubbox(Pack.Meta.Field.Bounds ph)
        {
            Pack.Meta meta = ph.pack.meta;
            ph._Cbbox = ph.field_bit == -1 ? meta._ebbox - 1 : ph.field_bit;
            int delta = ph.field_bit == -1 ? BitUtils.first_1(ph.pack.data, meta._ebbox, ph._ybbox - meta._ebbox, false) : BitUtils.first_1(ph.pack.data, ph.field_bit + 1, ph._ybbox - (ph.field_bit + 1), false);
            if(delta == -1)
            {
                ph._lbbox = ph._Dbbox;
                ph._Cbbox = ph.field_bit;
                ph._cbbox = ph._ybbox;
                Debug.Assert(ph._lbbox == ph.pack.data.Length);
            }
            else
            {
                int _ybbox_FX = ph._ybbox, _Zbbox_FX = ph._Zbbox, _Bbbox_FX = ph._Bbbox, _Dbbox_FX = ph._Dbbox, item_size_FX = ph.field_size;
                for(int max = meta._ebbox + meta.fields.Length - 1;;)
                {
                    ph._Cbbox += delta + 1;
                    ph._Zbbox = ph._ybbox;
                    ph._Bbbox = ph._Dbbox;
                    Pack.Meta.Field fld = meta.fields[ph._Cbbox - meta._ebbox];
                    if(0 < fld.length)
                        if(0 < fld.size)
                            ph._Dbbox += fld._wbbox * fld.length * fld.size;
                        else
                            ph._ybbox += fld._wbbox * fld.length * fld.size;
                    else setBounds(fld, ph);
                    if(ph._Cbbox == max || (delta = BitUtils.first_1(ph.pack.data, ph._Cbbox + 1, ph._ybbox - (ph._Cbbox + 1), false)) == -1) break;
                }
                ph._lbbox = ph._Dbbox;
                ph._cbbox = ph._ybbox;
                Debug.Assert(ph._lbbox == ph.pack.data.Length);
                ph._ybbox = _ybbox_FX;
                ph._Zbbox = _Zbbox_FX;
                ph._Bbbox = _Bbbox_FX;
                ph._Dbbox = _Dbbox_FX;
                ph.field_size = item_size_FX;
            }
        }

        public static bool insert_field(Pack.Meta.Field.Bounds.Inside ph, int fbit, int items, params int[] var_dims)
        {
            if(visit_field(ph, fbit)) return false;
            Pack.Meta.Field fld = ph.pack.meta.fields[fbit - ph.pack.meta._ebbox];
            if(fld.type == 0)
            {
                if(0 < fld.size)
                {
                    insert(ph, fbit, 0, fld._wbbox * fld.length * fld.size);
                    ph._Dbbox = (ph.BYTE = ph._Bbbox) + fld._wbbox * (ph.items = fld.length) * (ph.field_size = fld.size);
                }
                else
                {
                    insert(ph, fbit, fld._wbbox * fld.length * -fld.size, 0);
                    ph.BIT = ph._ybbox = ph._Zbbox + fld._wbbox * (ph.items = fld.length) * fld.size;
                }
                return true;
            }
            int total = fld._wbbox;
            if(var_dims != null)
                for(int i = var_dims.Length - 1; -1 < i; i--)
                    total *= var_dims[i];
            switch(fld.type)
            {
                case 1:
                    insert(ph, fbit, fld._Rbbox, total * -fld.length * fld.size);
                    break;
                case 2:
                case 6:
                case 8:
                case 12:
                    insert(ph, fbit, fld._Rbbox, 0);
                    break;
                case 3:
                    insert(ph, fbit, fld._Rbbox, total * items * fld.size);
                    BitUtils.set_bits((uint) items, -fld.length, ph.pack.data, ph._Zbbox - fld._Rbbox);
                    break;
                case 4:
                    insert(ph, fbit, fld._Rbbox, 0);
                    BitUtils.set_bits((uint) items, -fld.length, ph.pack.data, ph._Zbbox - fld._Rbbox + 2 * fld._sbbox);
                    break;
                case 5:
                case 11:
                    insert(ph, fbit, fld._Rbbox + total * -fld.length, 0);
                    break;
                case 7:
                    insert(ph, fbit, fld._Rbbox + total * -fld.length * fld.size, 0);
                    break;
                case 9:
                    insert(ph, fbit, fld._Rbbox + total * items * fld.size, 0);
                    BitUtils.set_bits((uint) items, -fld.length, ph.pack.data, ph._Zbbox - fld._Rbbox);
                    break;
                case 10:
                    insert(ph, fbit, fld._Rbbox, 0);
                    BitUtils.set_bits((uint) items, -fld.length, ph.pack.data, ph._Zbbox - fld._Rbbox + 2 * fld._sbbox);
                    break;
                default:
                    Debug.Assert(false);
                    return false;
            }
            ph._hbbox = total;
            if(0 < fld._bbbox)
            {
                if(ph.D == null || ph.D.Length < fld.dims.Length) ph.D = new int[fld.dims.Length];
                else
                    for(int i = ph.D.Length - 1; -1 < i;)
                        ph.D[i--] = 0;
                for(int i = 0, k = 0, bit = ph._Zbbox; i < fld.dims.Length; i++)
                    if(fld.dims[i] < 1)
                    {
                        ph.D[i] = var_dims[k];
                        BitUtils.set_bits((uint) var_dims[k], -fld.dims[i], ph.pack.data, bit -= -fld.dims[i]);
                        k++;
                    }
                    else
                        ph.D[i] = fld.dims[i];
            }
            setBounds(fld, ph);
            init(fld, ph);
            return true;
        }

        public static bool try_visit_item(Pack.Meta.Field.Bounds.Inside ph, int item)
        {
            Pack.Meta.Field fld = ph.field;
            switch(fld.type)
            {
                default:
                    Debug.Assert(false);
                    return false;
                case 2:
                case 4:
                    if(ph.field_item == item) return true;
                    if(ph._Ebbox == 0 || item < ph._nbbox || ph._nbbox + ph._Ebbox <= item) return false;
                    int bit = ph._Zbbox - fld._Rbbox - 1 - (item - ph._nbbox);
                    if((ph.pack.data[bit >> 3] & 1 << (bit & 7)) == 0) return false;
                    if(item < ph.field_item)
                    {
                        ph.field_item = ph._nbbox;
                        ph.BIT = ph._Zbbox - fld._Rbbox - 1;
                        ph.BYTE = ph._Bbbox;
                    }
                    else
                    {
                        ph.BIT--;
                        ph.field_item++;
                        ph.BYTE += ph.items * fld.size;
                    }
                    for(int bytes = ph.items * fld.size;; ph.field_item++, ph.BIT--)
                        if((ph.pack.data[ph.BIT >> 3] & 1 << (ph.BIT & 7)) != 0)
                            if(ph.field_item == item)
                                break;
                            else
                                ph.BYTE += bytes;
                    return true;
                case 5:
                    if(ph._hbbox - 1 < item || BitUtils.get_bits(ph.pack.data, ph._Zbbox - fld._Rbbox - (item + 1) * -fld.length, -fld.length) == 0) return false;
                    if(item < ph.field_item)
                    {
                        ph.field_item = 0;
                        ph.BIT = ph._Zbbox - fld._Rbbox;
                        ph.items = (int) BitUtils.get_bits(ph.pack.data, ph.BIT -= -fld.length, -fld.length);
                        ph.BYTE = ph._Bbbox;
                    }
                    for(; ph.field_item < item; ph.BYTE += ph.items * fld.size, ph.items = (int) BitUtils.get_bits(ph.pack.data, ph.BIT -= -fld.length, -fld.length), ph.field_item++) ;
                    return true;
                case 6:
                    if(ph.field_item == item) return true;
                    if(item < ph._nbbox || ph._nbbox + ph._Ebbox <= item) return false;
                    int field_item = ph.field_item;
                    bit = ph.BIT;
                    int BYTE = ph.BYTE;
                    if(item < field_item)
                    {
                        field_item = ph._nbbox;
                        bit = ph._Zbbox - fld._Rbbox - 1;
                        BYTE = ph._Bbbox;
                    }
                    else
                    {
                        bit--;
                        field_item++;
                        BYTE += ph.items * fld.size;
                    }
                    for(int len_bits = -fld.length;; bit--, field_item++)
                        if((ph.pack.data[bit >> 3] & 1 << (bit & 7)) != 0)
                        {
                            bit -= len_bits;
                            if(field_item == item)
                            {
                                ph.field_item = field_item;
                                ph.BYTE = BYTE;
                                ph.BIT = bit;
                                ph.items = (int) BitUtils.get_bits(ph.pack.data, bit, len_bits);
                                return true;
                            }
                            BYTE += (int) BitUtils.get_bits(ph.pack.data, bit, len_bits) * fld.size;
                        }
                        else if(field_item == item)
                            return false;
                case 8:
                case 10:
                    if(ph.field_item == item) return true;
                    if(item < ph._nbbox || ph._nbbox + ph._Ebbox <= item) return false;
                    field_item = ph.field_item;
                    bit = ph.BIT;
                    if(item < field_item)
                    {
                        field_item = ph._nbbox;
                        bit = ph._Zbbox - fld._Rbbox - 1;
                    }
                    else
                    {
                        bit--;
                        field_item++;
                    }
                    for(int bits = ph.items * fld.size;; bit--, field_item++)
                        if((ph.pack.data[bit >> 3] & 1 << (bit & 7)) != 0)
                        {
                            bit -= bits;
                            if(field_item == item)
                            {
                                ph.field_item = item;
                                ph.BIT = bit;
                                return true;
                            }
                        }
                        else if(field_item == item)
                            return false;
                case 11:
                    if(ph._hbbox - 1 < item) return false;
                    field_item = ph.field_item;
                    bit = ph.BIT;
                    if(item < field_item)
                    {
                        field_item = -1;
                        bit = ph._Zbbox - fld._Rbbox;
                    }
                    else if(item == ph.field_item)
                        return true;
                    int items;
                    do
                    {
                        int bits = fld.size * (items = (int) BitUtils.get_bits(ph.pack.data, bit -= -fld.length, -fld.length));
                        bit -= bits;
                        field_item++;
                    }
                    while(field_item < item);
                    if(items == 0) return false;
                    ph.BIT = bit;
                    ph.items = items;
                    ph.field_item = field_item;
                    return true;
                case 12:
                    if(ph.field_item == item) return true;
                    if(item < ph._nbbox || ph._nbbox + ph._Ebbox <= item) return false;
                    field_item = ph.field_item;
                    bit = ph.BIT;
                    if(item < field_item)
                    {
                        field_item = ph._nbbox;
                        bit = ph._Zbbox - fld._Rbbox - 1;
                    }
                    else
                    {
                        bit--;
                        field_item++;
                    }
                    for(int len_bits = -fld.length;; bit--, field_item++)
                        if((ph.pack.data[bit >> 3] & 1 << (bit & 7)) != 0)
                        {
                            bit -= len_bits;
                            if(field_item == item)
                            {
                                ph.field_item = field_item;
                                ph.items = (int) BitUtils.get_bits(ph.pack.data, bit, len_bits);
                                ph.BIT = bit - ph.items * fld.size;
                                return true;
                            }
                            bit -= (int) BitUtils.get_bits(ph.pack.data, bit, len_bits) * fld.size;
                        }
                        else if(field_item == item)
                            return false;
            }
        }


        public static void insert_item(Pack.Meta.Field.Bounds.Inside ph, int item, int items)
        {
            Pack.Meta.Field fld = ph.field;
            switch(fld.type)
            {
                default:
                    Debug.Assert(false);
                    return;
                case 2:
                case 4:
                    if(item < ph._nbbox || ph._Ebbox == 0)
                    {
                        ph.BIT = ph._Zbbox - fld._Rbbox;
                        ph.BYTE = ph._Bbbox;
                        int ins_items = ph._Ebbox == 0 ? 1 : ph._nbbox - item;
                        insert(ph, ph.field_bit, ins_items, ph.items * fld.size);
                        ph.BIT = ph._Zbbox - fld._Rbbox - 1;
                        BitUtils.set_bits((uint)(ph._nbbox = item), fld._sbbox, ph.pack.data, ph._Zbbox - fld._Rbbox + fld._sbbox);
                        BitUtils.set_bits((uint)(ph._Ebbox += ins_items), fld._sbbox, ph.pack.data, ph._Zbbox - fld._Rbbox);
                    }
                    else if(item < ph._nbbox + ph._Ebbox)
                    {
                        if(item < ph.field_item)
                        {
                            ph.field_item = ph._nbbox;
                            ph.BIT = ph._Zbbox - fld._Rbbox - 1;
                            ph.BYTE = ph._Bbbox;
                        }
                        else
                        {
                            ph.BIT--;
                            ph.field_item++;
                            ph.BYTE += ph.items * fld.size;
                        }
                        for(int bytes = ph.items * fld.size; ph.field_item < item; ph.field_item++, ph.BIT--)
                            if((ph.pack.data[ph.BIT >> 3] & 1 << (ph.BIT & 7)) != 0)
                                ph.BYTE += bytes;
                        Debug.Assert((ph.pack.data[ph.BIT >> 3] & 1 << (ph.BIT & 7)) == 0);
                        insert(ph, ph.field_bit, 0, ph.items * fld.size);
                    }
                    else
                    {
                        int ins_items = item + 1 - (ph._nbbox + ph._Ebbox);
                        ph.BIT = ph._ybbox;
                        ph.BYTE = ph._Dbbox;
                        insert(ph, ph.field_bit, ins_items, ph.items * fld.size);
                        ph.BIT = ph._ybbox;
                        ph.BYTE = ph._Dbbox - ph.items * fld.size;
                        BitUtils.set_bits((uint)(ph._Ebbox += ins_items), fld._sbbox, ph.pack.data, ph._Zbbox - fld._Rbbox);
                    }
                    ph.pack.data[ph.BIT >> 3] |= (byte)(1 << (ph.BIT & 7));
                    break;
                case 5:
                    if(ph._hbbox - 1 < item) throw new Exception("No room for item=" + item + ". The _hbbox =" + ph._hbbox);
                    if(item < ph.field_item)
                    {
                        ph.field_item = 0;
                        ph.BIT = ph._Zbbox - fld._Rbbox;
                        ph.BYTE = ph._Bbbox;
                        ph.items = (int) BitUtils.get_bits(ph.pack.data, ph.BIT -= -fld.length, -fld.length);
                    }
                    for(; ph.field_item < item; ph.BYTE += ph.items * fld.size, ph.items = (int) BitUtils.get_bits(ph.pack.data, ph.BIT -= -fld.length, -fld.length), ph.field_item++) ;
                    if(ph.items != 0) throw new Exception("Already allocated");
                    insert(ph, ph.field_bit, 0, items * fld.size);
                    BitUtils.set_bits((uint) items, -fld.length, ph.pack.data, ph.BIT);
                    ph.items = items;
                    break;
                case 6:
                    int len_bits = -fld.length;
                    if(item < ph._nbbox || ph._Ebbox == 0)
                    {
                        ph.BIT = ph._Zbbox - fld._Rbbox;
                        ph.BYTE = ph._Bbbox;
                        int ins_items = ph._Ebbox == 0 ? 1 : ph._nbbox - item;
                        insert(ph, ph.field_bit, ins_items + len_bits, items * fld.size);
                        ph.BIT = ph._Zbbox - fld._Rbbox - 1;
                        BitUtils.set_bits((uint)(ph._nbbox = item), fld._sbbox, ph.pack.data, ph._Zbbox - fld._Rbbox + fld._sbbox);
                        BitUtils.set_bits((uint)(ph._Ebbox += ins_items), fld._sbbox, ph.pack.data, ph._Zbbox - fld._Rbbox);
                    }
                    else if(item < ph._nbbox + ph._Ebbox)
                    {
                        if(item < ph.field_item)
                        {
                            ph.field_item = ph._nbbox;
                            ph.BIT = ph._Zbbox - fld._Rbbox - 1;
                            ph.BYTE = ph._Bbbox;
                        }
                        else
                        {
                            ph.BIT--;
                            ph.field_item++;
                            ph.BYTE += ph.items * fld.size;
                        }
                        for(; ph.field_item < item; ph.BIT--, ph.field_item++)
                            if((ph.pack.data[ph.BIT >> 3] & 1 << (ph.BIT & 7)) != 0)
                            {
                                ph.BIT -= len_bits;
                                ph.items = (int) BitUtils.get_bits(ph.pack.data, ph.BIT, len_bits);
                                ph.BYTE += ph.items * fld.size;
                            }
                        Debug.Assert((ph.pack.data[ph.BIT >> 3] & 1 << (ph.BIT & 7)) == 0);
                        insert(ph, ph.field_bit, len_bits, items * fld.size);
                    }
                    else
                    {
                        int ins_items = item - (ph._nbbox + ph._Ebbox) + 1;
                        ph.BIT = ph._ybbox;
                        ph.BYTE = ph._Dbbox;
                        insert(ph, ph.field_bit, ins_items + len_bits, items * fld.size);
                        ph.BIT = ph._ybbox + len_bits;
                        ph.BYTE = ph._Dbbox - items * fld.size;
                        BitUtils.set_bits((uint)(ph._Ebbox += ins_items), fld._sbbox, ph.pack.data, ph._Zbbox - fld._Rbbox);
                    }
                    ph.pack.data[ph.BIT >> 3] |= (byte)(1 << (ph.BIT & 7));
                    BitUtils.set_bits((uint)(ph.items = items), len_bits, ph.pack.data, ph.BIT -= len_bits);
                    break;
                case 8:
                case 10:
                    int bits = ph.items * fld.size;
                    if(ph.field_item == item) throw new Exception();
                    if(item < ph._nbbox || ph._Ebbox == 0)
                    {
                        ph.BIT = ph._Zbbox - fld._Rbbox;
                        ph.BYTE = ph._Bbbox;
                        int ins_items = ph._Ebbox == 0 ? 1 : ph._nbbox - item;
                        insert(ph, ph.field_bit, ins_items + bits, 0);
                        ph.BIT = ph._Zbbox - fld._Rbbox - 1;
                        BitUtils.set_bits((uint)(ph._nbbox = item), fld._sbbox, ph.pack.data, ph._Zbbox - fld._Rbbox + fld._sbbox);
                        BitUtils.set_bits((uint)(ph._Ebbox += ins_items), fld._sbbox, ph.pack.data, ph._Zbbox - fld._Rbbox);
                    }
                    else if(item < ph._nbbox + ph._Ebbox)
                    {
                        if(item < ph.field_item)
                        {
                            ph.field_item = ph._nbbox;
                            ph.BIT = ph._Zbbox - fld._Rbbox - 1;
                        }
                        else
                        {
                            ph.BIT--;
                            ph.field_item++;
                        }
                        for(; ph.field_item < item; ph.BIT--, ph.field_item++)
                            if((ph.pack.data[ph.BIT >> 3] & 1 << (ph.BIT & 7)) != 0)
                                ph.BIT -= bits;
                        Debug.Assert((ph.pack.data[ph.BIT >> 3] & 1 << (ph.BIT & 7)) == 0);
                        insert(ph, ph.field_bit, bits, 0);
                    }
                    else
                    {
                        int ins_items = item + 1 - (ph._nbbox + ph._Ebbox);
                        ph.BIT = ph._ybbox;
                        insert(ph, ph.field_bit, ins_items + bits, 0);
                        ph.BIT = ph._ybbox + bits;
                        BitUtils.set_bits((uint)(ph._Ebbox += ins_items), fld._sbbox, ph.pack.data, ph._Zbbox - fld._Rbbox);
                    }
                    ph.pack.data[ph.BIT >> 3] |= (byte)(1 << (ph.BIT & 7));
                    ph.BIT -= bits;
                    break;
                case 11:
                    if(ph._hbbox - 1 < item) throw new Exception("No room for item=" + item + ". The _hbbox =" + ph._hbbox);
                    if(item < ph.field_item)
                    {
                        ph.field_item = -1;
                        ph.BIT = ph._Zbbox - fld._Rbbox;
                    }
                    if(ph.field_item < item)
                        do
                        {
                            bits = fld.size * (int) BitUtils.get_bits(ph.pack.data, ph.BIT -= -fld.length, -fld.length);
                            ph.BIT -= bits;
                            ph.field_item++;
                        }
                        while(ph.field_item < item);
                    if(BitUtils.get_bits(ph.pack.data, ph.BIT, -fld.length) != 0) throw new Exception("Already allocated");
                    insert(ph, ph.field_bit, items * fld.size, 0);
                    BitUtils.set_bits((uint) items, -fld.length, ph.pack.data, ph.BIT);
                    ph.BIT -= items * fld.size;
                    ph.items = items;
                    break;
                case 12:
                    len_bits = -fld.length;
                    if(item < ph._nbbox || ph._Ebbox == 0)
                    {
                        ph.BIT = ph._Zbbox - fld._Rbbox;
                        int ins_items = ph._Ebbox == 0 ? 1 : ph._nbbox - item;
                        insert(ph, ph.field_bit, ins_items + len_bits + items * fld.size, 0);
                        ph.BIT = ph._Zbbox - fld._Rbbox - 1;
                        BitUtils.set_bits((uint)(ph._nbbox = item), fld._sbbox, ph.pack.data, ph._Zbbox - fld._Rbbox + fld._sbbox);
                        BitUtils.set_bits((uint)(ph._Ebbox += ins_items), fld._sbbox, ph.pack.data, ph._Zbbox - fld._Rbbox);
                    }
                    else if(item < ph._nbbox + ph._Ebbox)
                    {
                        if(item < ph.field_item)
                        {
                            ph.field_item = ph._nbbox;
                            ph.BIT = ph._Zbbox - fld._Rbbox - 1;
                        }
                        else
                        {
                            ph.BIT--;
                            ph.field_item++;
                        }
                        for(; ph.field_item < item; ph.BIT--, ph.field_item++)
                            if((ph.pack.data[ph.BIT >> 3] & 1 << (ph.BIT & 7)) != 0)
                            {
                                ph.BIT -= len_bits;
                                ph.items = (int) BitUtils.get_bits(ph.pack.data, ph.BIT, len_bits);
                                ph.BIT -= ph.items * fld.size;
                            }
                        Debug.Assert((ph.pack.data[ph.BIT >> 3] & 1 << (ph.BIT & 7)) == 0);
                        insert(ph, ph.field_bit, len_bits + items * fld.size, 0);
                    }
                    else
                    {
                        int ins_items = item - (ph._nbbox + ph._Ebbox) + 1;
                        ph.BIT = ph._ybbox;
                        insert(ph, ph.field_bit, ins_items + len_bits + items * fld.size, 0);
                        ph.BIT = ph._ybbox + items * fld.size + len_bits;
                        BitUtils.set_bits((uint)(ph._Ebbox += ins_items), fld._sbbox, ph.pack.data, ph._Zbbox - fld._Rbbox);
                    }
                    ph.pack.data[ph.BIT >> 3] |= (byte)(1 << (ph.BIT & 7));
                    BitUtils.set_bits((uint)(ph.items = items), len_bits, ph.pack.data, ph.BIT -= len_bits);
                    ph.BIT -= items * fld.size;
                    break;
            }
            ph.field_item = item;
        }



        public static bool try_visit_field(Pack.Meta.Field.Bounds.Inside ph, int fbit) { return !(ph.wasNULL = (ph._ybbox <= fbit || (ph.pack.data[fbit >> 3] & 1 << (fbit & 7)) == 0 || !visit_field(ph, fbit))); }

    }
}